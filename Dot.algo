ALGORITHM algorithm_name
VAR
    T1 : ARRAY_OF INTEGER[N];
    T2 : ARRAY_OF INTEGER[N];
    i,N : INTEGER;
    ps : FLOAT;
BEGIN

    FOR i FROM 0 TO N-1  DO
        Write ("Give the", i+1 "element for T1" )
        read (T1[i])
        Write ("Give the", i+1 "element for T2" )
        read (T2[i])
    END_FOR
    FOR i FROM 0 TO N-1  DO
        ps := ps + T1[i]*T2[j]
    END_FOR
    Write("the sum of all elements is : ",ps)
END

